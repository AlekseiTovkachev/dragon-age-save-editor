# QA — Test and Verify

Follow `workflows/qa.md`.

Read `AGENTS.md`, `docs/testing.md`, and `skills/testing-and-coverage/SKILL.md` before starting.

Run `npm run verify` first. Then `npm run smoke` for workflow/UI changes.
Do not mutate original save fixtures.

**User request:** $ARGUMENTS
