# Diagnose — Bug Investigation

Follow `workflows/diagnose.md`.

Reproduce before hypothesizing. Read the relevant skill file for the area (editing-patterns, save-format, frontend-workflows, command-contract) before reading code.

Fix minimally. Add a regression test. Verify with `npm run verify`.

Do not mutate original save fixtures.

**User request:** $ARGUMENTS
